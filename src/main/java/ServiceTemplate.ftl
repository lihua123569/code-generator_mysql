package ${info.servicePackage};

import ${info.modelPackage}.${info.modelName};
import com.liepaiwang.generic.GenericService;

public interface ${info.modelName}Service extends GenericService<${info.modelName}, String> {
 
    
}